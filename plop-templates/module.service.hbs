import type * as z from "zod";
import type { db as DBType } from "@/drizzle/db";
import type { {{name}}Schema } from "./{{name}}.schema";

export class {{pascalCase name}}Service {
	constructor(private db: typeof DBType) {}

	async get({
		userId,
	}: z.infer<typeof {{name}}Schema.get.input> & {
		userId: string;
	}): Promise<z.infer<typeof {{name}}Schema.get.output>> {
		// Implementation
		return {}
    }
}