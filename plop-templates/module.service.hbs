import type * as z from "zod";
import type { db as DBType } from "@/drizzle/db";
import type { {{name}}Schema } from "./{{name}}.schema";

export class {{pascalCase name}}Service {
	constructor(private db: typeof DBType, private userId: string) {}

	async get({
		{{name}}Id
	}: z.infer<typeof {{name}}Schema.get.input>): Promise<z.infer<typeof {{name}}Schema.get.output>> {
		// Implementation
		return {}
    }
}